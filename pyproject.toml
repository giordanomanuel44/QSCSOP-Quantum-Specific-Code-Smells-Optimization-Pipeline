[project]
name = "qscsop-pipeline"
version = "0.1.0"
description = "Quantum Specific Code Smells Optimization Pipeline"
requires-python = ">=3.10"
dependencies = [
    "qiskit",
    "crewai",
    "pandas",
    "matplotlib",
    "seaborn",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-mock",
    "ruff==0.15.20",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "unit: test veloci e isolati, eseguiti sempre",
    "integration: test tra più componenti dello stesso modulo",
    "e2e: test costosi con Qiskit/LLM reali, eseguiti manualmente",
]
testpaths = ["tests"]
# La root del progetto in sys.path: qscsop_pipeline e' installato (layout src/), ma scripts/ no --
# e' tooling, non un package distribuito. Serve a importare scripts.synthetic_dataset nei test.
pythonpath = ["."]

[tool.ruff]
line-length = 100
extend-exclude = ["data"]

[tool.setuptools.packages.find]
where = ["src"]